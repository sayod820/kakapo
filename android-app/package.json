{
  "name": "kakapo-trade-android",
  "version": "1.2.145",
  "private": true,
  "description": "KAKAPO Trade — Android (локальный UI в APK, API kakappo.shop)",
  "scripts": {
    "sync": "npx cap sync android",
    "open": "npx cap open android",
    "build:debug": "cd android && .\\gradlew.bat assembleDebug",
    "build:release": "cd android && .\\gradlew.bat assembleRelease",
    "apk": "npm run sync && npm run build:debug"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/app": "^6.0.2",
    "@capacitor/core": "^6.2.0",
    "@capacitor/splash-screen": "^6.0.3",
    "@capacitor/status-bar": "^6.0.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0",
    "typescript": "^5.6.3"
  }
}
