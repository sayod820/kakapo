PORT=8000
DATA_DIR=./data
NODE_ENV=development
# Через запятую: продакшн-домен на Hetzner + localhost для разработки
CORS_ORIGINS=http://localhost:3001,https://ваш-домен.tj

# ИИ-ассистент админки (Google Gemini — бесплатный ключ в AI Studio)
# Не коммитьте настоящий ключ в git
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash
